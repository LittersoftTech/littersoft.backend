{
  "ConnectionStrings": {
    "SqlServer": "Server=tcp:littersoftdb.database.windows.net,1433;Initial Catalog=littersoft-dev;Persist Security Info=False;User ID=littersoftadmin;Password=$NewProject@12$;MultipleActiveResultSets=False;Encrypt=True;TrustServerCertificate=False;Connection Timeout=100;"
  },
  "AzureKeyVault": {
    "Enabled": false,
    "VaultUri": "https://littersoft.vault.azure.net/",
    "Secrets": {
      "SqlConnectionString": "SQLKey",
      "CosmosKey": "CosmosKey",
      "BlobStorageKey": "BlobStorageKey"
    }
  },

  "Notifications": {
    "_comment": [
      "Two Firebase projects, because the provider and pet-parent apps are registered separately.",
      "A device token issued by one project is rejected by the other (SENDER_ID_MISMATCH),",
      "so the notification's audience selects the credential.",
      "",
      "Supply the service-account JSON (Firebase Console > Project Settings > Service Accounts >",
      "Generate new private key) ONE of two ways:",
      "  CredentialsFilePath   - a local, gitignored file. For development.",
      "  CredentialsSecretName - a Key Vault secret name (or LocalSecrets:<name> when",
      "                          AzureKeyVault:Enabled is false). For deployed environments.",
      "The JSON contains a private key, so it must never be pasted into this file.",
      "",
      "In Azure, Application Settings bind as environment variables, so nested keys use a",
      "DOUBLE UNDERSCORE, not a colon:  Notifications__Provider__CredentialsSecretName"
    ],

    "Provider": {
      "ProjectId": "littersoftprovider",
      "CredentialsFilePath": "Secrets/littersoftprovider-firebase-adminsdk-fbsvc-54751b3ba7.json",
      "CredentialsSecretName": "FirebaseProviderServiceAccount"
    },

    "PetParent": {
      "ProjectId": "pawfrontparent-89296",
      "CredentialsFilePath": "Secrets/pawfrontparent-89296-firebase-adminsdk-fbsvc-d667471948.json",
      "CredentialsSecretName": "FirebaseParentServiceAccount"
    },

    "Android": {
      "_comment": "ChannelId must match a channel the app has already created — Android 8+ silently drops notifications naming an unknown channel. Icon is a drawable resource name bundled in the app, NOT a URL.",
      "ChannelId": "pawfront_default",
      "Icon": "ic_notification",
      "Color": "#FF7A00"
    },

    "Dispatch": {
      "BatchSize": 100,
      "MaxAttempts": 5,
      "LeaseMinutes": 5
    }
  }
}
